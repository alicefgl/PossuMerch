@model IEnumerable<PossuMerch.Data.RigaCarrello>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model._NomeP)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoP)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Prezzo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantita)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item._NomeP)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoP)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prezzo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantita)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdRigaCarrello">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdRigaCarrello">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdRigaCarrello">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
